{
  "name": "git-event-demo-node",
  "version": "1.0.0",
  "description": "Illustrates GitHub push/pull/merge using Express app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "build": "node build.js"
  },
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },

  "jtest":{

    "testEnvironment" : "node"
  }
}
